#include <iostream>
#include <vector>
using namespace std;

void helper(vector <int> arr, vector <int> sub, int ind){
  if (ind == arr.size()){
    for (int i = 0; i < sub.size(); i++){
      cout<<sub[i]<< " ";
    }
    cout<<endl;
    return;
  }

  //take
  sub.push_back(arr[ind]);
  helper(arr, sub, ind+1);
  
  //not take
  sub.pop_back();
  helper(arr, sub, ind+1);

  return;
}

int main(){
  vector <int> arr;
  int n;

  for (int i = 0; i < 3; i++){
    cin>>n;
    arr.push_back(n);
  }
  

  helper(arr, {}, 0);
}

