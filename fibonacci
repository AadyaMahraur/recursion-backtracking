#include <iostream>
using namespace std;

long long int fib(int n);

int main(){
  int N;
  cin >> N;
  cout << fib(N);
}

//fibonacci of nth number ex. fib(6th num) = 5
long long int fib(int n){
  if (n == 1){
    return 0;
  }
  if (n == 2){
    return 1;
  }
  return fib(n-1) + fib(n-2);
}
